"""
The simulator, the poses, and the frames that come back.

    proc_scene.py       a bare room built from assets; also `open_scene` for
                        THOR's own FloorPlans
    robot_controller.py base + camera on AI2-THOR
    real_robot.py       frames from a real camera, wearing the same interface
    nvs_lemniscate.py   the sweep: trajectory, third-party render, parking
    nvs_seva.py         Stable Virtual Camera in the seam `sweep` opened
"""
